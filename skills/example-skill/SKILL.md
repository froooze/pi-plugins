---
name: example-skill
description: Example skill showing the SKILL.md layout. Replace with your own workflow, scripts, and references.
---

# Example Skill

Replace this with your own instructions. Skills load on-demand: only `name` + `description` are always in context, the full file loads when the task matches or via `/skill:example-skill`.

## Usage

```bash
/skill:example-skill do something
```

## References

Put helper scripts in `scripts/` and detailed docs in `references/`, link them with relative paths:

- [Reference](references/reference.md)
