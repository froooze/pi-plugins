# Keep `pi update` output clean: this package is installed via `npm install`
# inside the pi package manager (no --no-fund/--no-audit flags), so disable
# the funding/audit noise at the source. Studied effect only; installs the
# exact same bits.
fund=false
audit=false
